A macOS menubar application that detects when an external drive has been connected. When connected it checks to see if Spotlight indexing is enabled. If so, it disables it.
Has and activity log to verify.
Keeps a list of histroical drives that it disabled indexing on.
